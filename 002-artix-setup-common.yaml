service_paths:
  service_path: "/run/runit/service/"
  sv_path: "/etc/runit/sv/"

pipewire:
  setup_service:
    packages:
      command: "sudo pacman -S {package} --needed --noconfirm"
      package:
        - "world/pipewire"
    path_init: true
    run_file:
      content: |
        #!/bin/sh
        exec /usr/bin/pipewire
    log_file:
      content: |
        #!/bin/sh
        exec svlogd -tt /var/log/pipewire
    service_init: true

pipewire-pulse:
  setup_service:
    packages:
      command: "sudo pacman -S {package} --needed --noconfirm"
      package:
        - "world/pipewire-pulse"
    path_init: true
    run_file:
      content: |
        #!/bin/sh
        exec /usr/bin/pipewire-pulse
    log_file:
      content: |
        #!/bin/sh
        exec svlogd -tt /var/log/pipewire-pulse
    service_init: true
    
pipewire-alsa:
  setup_service:
    packages:
      command: "sudo pacman -S {package} --needed --noconfirm"
      package:
        - "world/pipewire-alsa"
    path_init: true
    run_file:
      content: |
        #!/bin/sh
        exec /usr/bin/pipewire-alsa
    log_file:
      content: |
        #!/bin/sh
        exec svlogd -tt /var/log/pipewire-alsa
    service_init: true
